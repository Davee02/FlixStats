version: "3"
services:
    web:
        build: .
        ports:
            - "5000:5000"
        depends_on:
            - db
        environment:
          ASPNETCORE_ENVIRONMENT: "Production"
    db:
        image: "mcr.microsoft.com/mssql/server"
        environment:
            SA_PASSWORD: "Error404!"
            ACCEPT_EULA: "Y"