@model FlixStats.Models.ViewModels.NetflixStatsViewModel

<div class="per-day-sub" id="per-day-sub">
    Tap on a bar for detailed information
</div>

<div class="chart-container" style="height: @(Model.ViewedHoursPerDayChart.Data.Labels.Count * 10)px">
    <canvas id="timePerDateBarChart" role="img">
        <p>Your browser sadly does not support the canvas element. This means no charts can be displayed :(</p>
    </canvas>
</div>